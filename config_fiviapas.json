{
  "data": {
    "desc": "defines the data",
    "train_dataset": "/cluster/home/fiviapas/data/numpy/",
    "test_dataset": "/cluster/home/fiviapas/data/numpy/",
    "batch_size": 16,
    "shuffle": true,
    "train_set_percentage": 1
  },
  "model": {
    "desc": "defines the model",
    "num_classes": 5,
    "freeze_fairseq": true
  },
  "general": {
    "desc": "defines general information for the training run and which optimizer and loss_function are taken",
    "num_workers": 8,
    "epochs": 300,
    "optimizer": "adam",
    "loss_function": "nllLoss",
    "log_interval": 5,
    "model_save_interval": 2
  },
  "optimizers": {
    "desc": "defines usable optimizer and their needed inputs",
    "adam": {
      "lr": 0.001,
      "weight_decay": 0.0001
    }
  },
  "scheduler": {
    "desc": "defines params for scheduler",
    "step_size": 80,
    "gamma": 0.1
  }
}
